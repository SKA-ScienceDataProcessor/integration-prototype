# Requirements for ska-sip
#
# Install with:
#   $ pip3 install -U -r requirements.txt

# Core requirements
docker>=2.1.0
numpy>=1.11.2
pyzmq>=16.0.0
rpyc>=3.3.0
simplejson>=3.10.0
PyYaml>=3.12
requests>=2.17.3

# Needed for Jenkins & python3 setup.py test
unittest-xml-reporting>=2.1.0

# Task requirements
# FIXME Disabled to make sure these are easy to pull out of core SIP.
# spead2>=1.3.2
# pyftpdlib>=1.4.0
# astropy>=2.0
