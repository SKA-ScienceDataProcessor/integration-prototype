version: "3.6"

services:
  log_spammer:
    image: skasip/log_spammer
    logging:
      driver: "json-file"
      options:
        max-size: 10m
        max-file: "3"
        labels: "testing_log_driver"
