version: 3.7

services:

  mock_workflow_stage:
    image: skasip/mock_workflow_stage:1.0.0
    command: >
      '{
        "log_interval": 1.0,
        "timeout": 30.0
      }'
    delpoy:
      mode: replicated
      replicas: 1
      resources:
        limits:
          cpus: '1.0'
          memory: 1024M
    networks:
      - hostnet


networks:
  hostnet:
    external: true
    name: host
