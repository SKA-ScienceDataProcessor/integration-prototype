version: '3.7'

services:

  setup_stage:
    image: skasip/mock_workflow_stage:test
    command: {{ "stage.args" }}
    deploy:
      mode: replicated
      replicas: 1
      resources:
        limits:
          cpus: '1.0'
          memory: 1024M
{#    logging:#}
{#      driver: fluentd#}
{#      options:#}
{#        tag: {{ '"{{.ImageName}}/{{.Name}}/{{.ID}}"' }}#}
