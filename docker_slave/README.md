Skeleton implementation of a slave controller that runs in a Docker
container.

To build the docker image issue this command from this file's parent
directory:

```
docker build -f docker_slave/Dockerfile -t slave_controller .
```
